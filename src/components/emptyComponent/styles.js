import { StyleSheet } from 'react-native'
import { W } from '../../theme'

export default StyleSheet.create({
    container: {
        flex: 1, 
        alignItems: 'center', 
        justifyContent: 'center'
    },
    bodyText:{
        color:'black',
        fontSize:W(25)
    }
})
import {StyleSheet} from 'react-native'
import colors from '../../theme/colors'
import {H} from '../../theme/index'
export default StyleSheet.create({
    container:{
        justifyContent:'center',
        alignItems:'center',
        marginVertical:H(20)
    },
    textBlack:{
        color:colors.black
    }
})